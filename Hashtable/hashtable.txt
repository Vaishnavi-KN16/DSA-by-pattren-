hash table is a data structure that stores data as key → value pairs and allows very fast insertion, deletion, and lookup (typically O(1) average time).
Hashtable<String, Integer> table = new Hashtable<>();
HashSet<Integer> set = new HashSet<>(); ( used for checking duplicates)
put()
get()
containsKey()
remove()
keySet()
values()
hash set and its uses 
1.removing duplicates 

ArrayList<Integer> list = new ArrayList<>();

list.add(1);
list.add(1);
list.add(2);
list.add(3);
list.add(2);

HashSet<Integer> set = new HashSet<>(list);

System.out.println(set);
2. finding duplicates 
int[] arr = {1,2,3,1,4,2};

HashSet<Integer> set = new HashSet<>();

for(int num : arr){
    if(set.contains(num)){
        System.out.println("Duplicate: " + num);
    }
    set.add(num);
}
Use HashSet when:

✅ You need unique elements

✅ You need fast searching

✅ You need to detect duplicates

✅ You need fast insertions and deletions
Don't use it when:

❌ You need elements in a specific order

❌ You need indexing like get(0)

❌ You need duplicate values


import java.util.HashSet;

class Solution {
    public int removeDuplicates(int[] nums) {
        HashSet<Integer> set = new HashSet<>(nums);
        return set;
        
    }
}
HashSet expects a Collection, but nums is an int[] array.
hash set will not accept the arrays only array list 